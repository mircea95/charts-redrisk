apiVersion: v1
kind: Service
metadata:
  name: {{ include "red-app.fullname" . }}
  labels:
    {{- include "red-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "red-app.selectorLabels" . | nindent 4 }}
